export class Language {
  public languageId:number;
  public language: string;

  constructor(languageId: number, language: string){
    this.languageId = languageId;
    this.language = language;
  }
}
